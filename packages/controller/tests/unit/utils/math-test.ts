import math from 'controller/utils/math';
import { module, test } from 'qunit';

module('Unit | Utility | math', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = math();
    assert.ok(result);
  });
});
