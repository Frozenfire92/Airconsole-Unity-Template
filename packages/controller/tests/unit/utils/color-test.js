import color from 'controller/utils/color';
import { module, test } from 'qunit';

module('Unit | Utility | color', function() {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = color();
    assert.ok(result);
  });
});
