import controllerToScreenMessages from 'controller/utils/controller-to-screen-messages';
import { module, test } from 'qunit';

module('Unit | Utility | controller-to-screen-messages', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = controllerToScreenMessages();
    assert.ok(result);
  });
});
