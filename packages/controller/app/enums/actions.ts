export enum ControllerToScreenActions {
  PressAnywhere,
  MakeSound,
  OpenCloseHelp,
  Input
}

export enum ScreenToControllerActions {
  UpdateState,
  UpdateColor,
  UpdateMessage,
  UpdateButtons,
  UpdateInput
}
